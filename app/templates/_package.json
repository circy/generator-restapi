{
  "name": "<%= appName %>",
  "version": "0.0.0",
  "main": "app.js",
  "author": {
    "name": "<%= developer %>"
  },
  "dependencies": {
    "body-parser": "^1.10.0",
    <% if (cors == true) { %>"cors": "^2.5.3",<% } %>
    <% if (jwt == true) { %>"express-jwt": "^0.5.1",<% } %>
    <% if (jwt == true) { %>"jsonwebtoken": "^1.3.0",<% } %>
    <% if (mongo == true) { %>"mongoose": "^3.8.20",<% } %>
    "express": "^4.10.6"
  }
}
