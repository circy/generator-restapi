{
  "name": "<%= appName %>",
  "version": "0.0.0",
  "main": "app.js",
  "dependencies": {
    "body-parser": "^1.10.0",
    <% if cors { %>"cors": "^2.5.3",<% } %>
    <% if jwt { %>"express-jwt": "^0.5.1",<% } %>
    <% if jwt { %>"jsonwebtoken": "^1.3.0",<% } %>
    <% if mongo { %>"mongoose": "^3.8.20",<% } %>
    "express": "^4.10.6"
  }
}
