{
  "name": "<%= appName %>",
  "version": "0.0.0",
  "main": "app.js",
  "author": {
    "name": "<%= developer %>"
  },
  "dependencies": {
    "body-parser": "^1.10.0",
    <% if (cors == true) { %>"cors": "latest",<% } %>
    <% if (jwt == true) { %>"express-jwt": "latest",<% } %>
    <% if (jwt == true) { %>"jsonwebtoken": "latest",<% } %>
    <% if (mongo == true) { %>"mongoose": "latest",<% } %>
    "express": "^4.10.6"
  }
}
